（1）main.py里面的太过拥挤。修改main.py，将里面的函数（函数对应功能不要改）按照功能进行模块划分，分别放到src/f"{module_file_name}"下的不同py文件中。
（2）函数太多预先定义的输出，比如wirite_excel中的headers，是直接形式写入在函数中，如果要更改写入excel中的headers，需要回到该函数中调节。
要求：
- summary_templete.md不变
- 实现我只需要改变review_templete中的AI_USER_PROMPT，例如改变要求输出字段的内容，加上一个新字段或减去一个输出字段，对应的修改CARD_TEMPLATE里面的内容，还有改变AI_READING_PROMPT里面的聚焦的研究主题，来达到对RIS文献组进行另一种文献卡片字段、excel headers的输出和研究主题分类总结。（即不需要进入函数去手动修改相关内容）
（3）添加不同的AI agent，比如chatgpt和claude，以满足可以切换不同的AI agent来分析文献。
（4）deepseek v4 pro的配置为最大思考模式。
（5）module_file_name可以分有：
- RIS_analysis
- remove_duplicates
- AI_cache_annotation
- read_templete
- output_module
- 后续的划分